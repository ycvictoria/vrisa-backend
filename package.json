{
  
  "dependencies": {
    "@supabase/supabase-js": "^2.86.2"
  },
  "scripts": {
    "gen": "node generate-migrations.js",
    "migrate:local": "npm run gen && supabase db reset",
    "migrate:prod": "npm run gen && supabase db push"
  }
   
}
